import fetch from 'node-fetch'; async function test() { const res = await fetch('http://localhost:3000/api/hr/leave', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ employeeNumber: '20261184', startDate: '2026-03-20', endDate: '2026-03-21', daysRequested: 2, reason: 'Test Reason', leaveType: 'ANNUAL' }) }); const text = await res.text(); console.log(res.status, text); } test();
