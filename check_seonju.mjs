import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findFirst({ where: { name: '½Å¼±ÁÖ' } }); console.log(JSON.stringify(user, null, 2)); } main().then(() => prisma.()).then(() => process.exit(0));
